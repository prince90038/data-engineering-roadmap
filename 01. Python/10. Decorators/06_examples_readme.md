Decorators examples

Files:
- basic_logging.py — simple logging decorator
- execution_time.py — measure execution time
- retry_decorator.py — parameterized retry decorator
- auth_decorator.py — simple auth check decorator
- validate_decorator.py — parameterized type validation decorator

Each file includes a module-level docstring with usage examples.
